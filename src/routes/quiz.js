const express = require('express');
const router = express.Router();

// Note: Quiz functionality has been moved to authenticated routes
// Use /api/v1/auth/daily-quiz, /api/v1/auth/quiz/answer, etc. for authenticated drivers
// Use /api/v1/admin/quiz/* for admin functionality

module.exports = router;